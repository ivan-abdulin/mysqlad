language: php
php:
    - '7.0'

sudo: required

services:
    - docker

install:
    - ls -Al
    - docker-compose --version
    - composer install
    - docker-compose build
    - docker-compose up -d
    - docker-compose ps

script: docker run mysqlad_nginx_php_fpm_1 /bin/sh -c "cd /usr/share/nginx/html && vendor/bin/codecept"