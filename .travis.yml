language: php
php:
    - '7.0'

sudo: required

services:
    - docker

install:
    - composer install
    - npm install
    - gulp deploy

script: vendor/bin/codecept run -c app/config/codeception.yml
